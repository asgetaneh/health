{% extends 'base.html.twig' %}

{% block title %}PlanAchievement{% endblock %}

{% block body %}
    <h1>PlanAchievement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plan_achievement.id }}</td>
            </tr>
            <tr>
                <th>AccomplishmentValue</th>
                <td>{{ plan_achievement.accomplishmentValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plan_achievement_index') }}">back to list</a>

    <a href="{{ path('plan_achievement_edit', {'id': plan_achievement.id}) }}">edit</a>

    {{ include('plan_achievement/_delete_form.html.twig') }}
{% endblock %}
