<form method="post" action="{{ path('performer_task_delete', {'id': performer_task.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ performer_task.id) }}">
    <button class="btn">Delete</button>
</form>
