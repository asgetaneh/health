{% extends 'base.html.twig' %}

{% block title %}TaskMeasurement index{% endblock %}

{% block body %}
    <h1>TaskMeasurement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for task_measurement in task_measurements %}
            <tr>
                <td>{{ task_measurement.id }}</td>
                <td>{{ task_measurement.name }}</td>
                <td>{{ task_measurement.description }}</td>
                <td>
                    <a href="{{ path('task_measurement_show', {'id': task_measurement.id}) }}">show</a>
                    <a href="{{ path('task_measurement_edit', {'id': task_measurement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('task_measurement_new') }}">Create new</a>
{% endblock %}
