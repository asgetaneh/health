{% extends 'base.html.twig' %}

{% block title %}TaskMeasurement{% endblock %}

{% block body %}
    <h1>TaskMeasurement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_measurement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ task_measurement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ task_measurement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('task_measurement_index') }}">back to list</a>

    <a href="{{ path('task_measurement_edit', {'id': task_measurement.id}) }}">edit</a>

    {{ include('task_measurement/_delete_form.html.twig') }}
{% endblock %}
