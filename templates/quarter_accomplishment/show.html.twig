{% extends 'base.html.twig' %}

{% block title %}QuarterAccomplishment{% endblock %}

{% block body %}
    <h1>QuarterAccomplishment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quarter_accomplishment.id }}</td>
            </tr>
            <tr>
                <th>AchievementValue</th>
                <td>{{ quarter_accomplishment.achievementValue }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quarter_accomplishment_index') }}">back to list</a>

    <a href="{{ path('quarter_accomplishment_edit', {'id': quarter_accomplishment.id}) }}">edit</a>

    {{ include('quarter_accomplishment/_delete_form.html.twig') }}
{% endblock %}
