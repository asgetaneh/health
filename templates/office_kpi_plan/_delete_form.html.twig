<form method="post" action="{{ path('office_kpi_plan_delete', {'id': office_kpi_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ office_kpi_plan.id) }}">
    <button class="btn">Delete</button>
</form>
