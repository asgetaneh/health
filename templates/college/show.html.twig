{% extends 'base.html.twig' %}

{% block title %}College{% endblock %}

{% block body %}
    <h1>College</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ college.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ college.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ college.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('college_index') }}">back to list</a>

    <a href="{{ path('college_edit', {'id': college.id}) }}">edit</a>

    {{ include('college/_delete_form.html.twig') }}
{% endblock %}
