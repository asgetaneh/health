{% extends 'base.html.twig' %}

{% block title %}Disablity{% endblock %}

{% block body %}
    <h1>Disablity</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ disablity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ disablity.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ disablity.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('disablity_index') }}">back to list</a>

    <a href="{{ path('disablity_edit', {'id': disablity.id}) }}">edit</a>

    {{ include('disablity/_delete_form.html.twig') }}
{% endblock %}
