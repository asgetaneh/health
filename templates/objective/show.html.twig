{% extends 'base.html.twig' %}

{% block title %}
	Show Objective
{% endblock %}

{% block body %}
	<div class="row">
		<div class="col-md-12 col-sm-12 widget-container-col" id="widget-container-col-8">
			<div class="widget-box widget-color-blue" id="widget-box-8">
				<div class="widget-header">
					<h5 class="widget-title bigger lighter">Show Objective
					</h5>

					<div class="widget-toolbar">
						<span class="badge badge-info">
							<a href="{{ path('objective_index') }}" class="btn btn-success">
								{% trans %}
								back
								{% endtrans %}
							</a>
						</span>
					</div>

				</div>

				<div class="widget-body">
					<div class="widget-toolbox" id="widget-toolbox-1">
						<div class="btn-toolbar"></div>
					</div>

					<div class="widget-main padding-16">

						<div class="table-responsive">
							<table class="table">
								<tbody>
									<tr>
										<th>Id</th>
										<td>{{ objective.id }}</td>
									</tr>
									<tr>
										<th>Name</th>
										<td>{{ objective.name }}</td>
									</tr>
									<tr>
										<th>OutPut</th>
										<td>{{ objective.outPut }}</td>
									</tr>
									<tr>
										<th>OutCome</th>
										<td>{{ objective.outCome }}</td>
									</tr>
									<tr>
										<th>Weight</th>
										<td>{{ objective.weight }}</td>
									</tr>

									<tr>
										<th>Status</th>
										{% if  objective.isActive %}

											<td>Active</td>
										{% else %}
											<td>InActive</td>
										{% endif %}

									</tr>
									<tr>
										<th>CreatedAt</th>
										<td>{{ objective.createdAt ? objective.createdAt|date('Y-m-d H:i:s') : '' }}</td>
									</tr>
								</tbody>
							</table>
							


							<div class="col-md-6">
								<a href="{{ path('objective_index', {'id': objective.id}) }}" class="btn btn-warning">edit</a>
							</div>
							<div class="col-md-6">
								{{ include('objective/_delete_form.html.twig') }}
							</div>

						</div>

					</div>

				</div>
			</div>
		</div>
	</div>


{% endblock %}
