{% extends 'base.html.twig' %}

{% block title %}UnitOfMeasurement{% endblock %}

{% block body %}
    <h1>UnitOfMeasurement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unit_of_measurement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ unit_of_measurement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ unit_of_measurement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unit_of_measurement_index') }}">back to list</a>

    <a href="{{ path('unit_of_measurement_edit', {'id': unit_of_measurement.id}) }}">edit</a>

    {{ include('unit_of_measurement/_delete_form.html.twig') }}
{% endblock %}
