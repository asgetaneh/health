<form method="post" action="{{ path('unit_of_measurement_delete', {'id': unit_of_measurement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unit_of_measurement.id) }}">
    <button class="btn">Delete</button>
</form>
