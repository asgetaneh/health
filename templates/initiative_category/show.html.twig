{% extends 'base.html.twig' %}

{% block title %}InitiativeCategory{% endblock %}

{% block body %}
    <h1>InitiativeCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ initiative_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ initiative_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ initiative_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('initiative_category_index') }}">back to list</a>

    <a href="{{ path('initiative_category_edit', {'id': initiative_category.id}) }}">edit</a>

    {{ include('initiative_category/_delete_form.html.twig') }}
{% endblock %}
