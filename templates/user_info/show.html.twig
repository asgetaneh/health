{% extends 'base.html.twig' %}

{% block title %}UserInfo{% endblock %}

{% block body %}
    <h1>UserInfo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_info.id }}</td>
            </tr>
            <tr>
                <th>FullName</th>
                <td>{{ user_info.fullName }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_info.email }}</td>
            </tr>
            <tr>
                <th>EmployeeNumber</th>
                <td>{{ user_info.employeeNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_info_index') }}">back to list</a>

    <a href="{{ path('app_user_info_edit', {'id': user_info.id}) }}">edit</a>

    {{ include('user_info/_delete_form.html.twig') }}
{% endblock %}
