{% extends 'base.html.twig' %}

{% block title %}Edit Permission
{% endblock %}
{% block maintitle %}
	Permission
{% endblock %}
{% block breadcrumb %}Edit Permission
{% endblock %}

{% block body %}

	<div
		class="row">
		<!-- [ sample-page ] start -->
		<div class="col-sm-12">
			<div class="card">

				<div class="card-header">
					<h5>Edit Permission</h5>
					<div class="card-header-right">
						<a href="{{ path('permission_index') }}" class="btn btn-success">back to list</a>


					</div>
				</div>
				<div class="card-body">
					{{ include('permission/_form.html.twig', {'button_label': 'Update'}) }}


					{# {{ include('permission/_delete_form.html.twig') }} #}
				</div>
			</div>
		</div>
	</div>


{% endblock %}
