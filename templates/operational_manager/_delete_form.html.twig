<form method="post" action="{{ path('operational_manager_delete', {'id': operational_manager.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ operational_manager.id) }}">
    <button class="btn">Delete</button>
</form>
