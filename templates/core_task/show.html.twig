{% extends 'base.html.twig' %}

{% block title %}CoreTask{% endblock %}

{% block body %}
    <h1>CoreTask</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ core_task.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ core_task.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('core_task_index') }}">back to list</a>

    <a href="{{ path('core_task_edit', {'id': core_task.id}) }}">edit</a>

    {{ include('core_task/_delete_form.html.twig') }}
{% endblock %}
