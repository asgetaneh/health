<form method="post" action="{{ path('sposnsorship_delete', {'id': sposnsorship.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sposnsorship.id) }}">
    <button class="btn">Delete</button>
</form>
