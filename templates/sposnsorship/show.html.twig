{% extends 'base.html.twig' %}

{% block title %}Sposnsorship{% endblock %}

{% block body %}
    <h1>Sposnsorship</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sposnsorship.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sposnsorship.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ sposnsorship.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sposnsorship_index') }}">back to list</a>

    <a href="{{ path('sposnsorship_edit', {'id': sposnsorship.id}) }}">edit</a>

    {{ include('sposnsorship/_delete_form.html.twig') }}
{% endblock %}
