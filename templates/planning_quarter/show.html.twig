{% extends 'base.html.twig' %}

{% block title %}PlanningQuarter{% endblock %}

{% block body %}
    <h1>PlanningQuarter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ planning_quarter.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ planning_quarter.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ planning_quarter.slug }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ planning_quarter.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ planning_quarter.createdAt ? planning_quarter.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('planning_quarter_index') }}">back to list</a>

    <a href="{{ path('planning_quarter_edit', {'id': planning_quarter.id}) }}">edit</a>

    {{ include('planning_quarter/_delete_form.html.twig') }}
{% endblock %}
