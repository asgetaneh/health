<form method="post" action="{{ path('planning_quarter_delete', {'id': planning_quarter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning_quarter.id) }}">
    <button class="btn">Delete</button>
</form>
