{% extends 'base.html.twig' %}

{% block title %}ProgramLevel{% endblock %}

{% block body %}
    <h1>ProgramLevel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program_level.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ program_level.name }}</td>
            </tr>
            <tr>
                <th>ShortName</th>
                <td>{{ program_level.shortName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ program_level.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('program_level_index') }}">back to list</a>

    <a href="{{ path('program_level_edit', {'id': program_level.id}) }}">edit</a>

    {{ include('program_level/_delete_form.html.twig') }}
{% endblock %}
