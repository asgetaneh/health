<form method="post" action="{{ path('program_level_delete', {'id': program_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ program_level.id) }}">
    <button class="btn">Delete</button>
</form>
