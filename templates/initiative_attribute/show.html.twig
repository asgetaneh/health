{% extends 'base.html.twig' %}

{% block title %}InitiativeAttribute{% endblock %}

{% block body %}
    <h1>InitiativeAttribute</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ initiative_attribute.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ initiative_attribute.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ initiative_attribute.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ initiative_attribute.createdAt ? initiative_attribute.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('initiative_attribute_index') }}">back to list</a>

    <a href="{{ path('initiative_attribute_edit', {'id': initiative_attribute.id}) }}">edit</a>

    {{ include('initiative_attribute/_delete_form.html.twig') }}
{% endblock %}
