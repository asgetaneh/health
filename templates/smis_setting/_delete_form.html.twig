<form method="post" action="{{ path('smis_setting_delete', {'id': smis_setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ smis_setting.id) }}">
    <button class="btn">Delete</button>
</form>
