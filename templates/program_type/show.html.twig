{% extends 'base.html.twig' %}

{% block title %}ProgramType{% endblock %}

{% block body %}
    <h1>ProgramType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ program_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ program_type.name }}</td>
            </tr>
            <tr>
                <th>ShortName</th>
                <td>{{ program_type.shortName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ program_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('program_type_index') }}">back to list</a>

    <a href="{{ path('program_type_edit', {'id': program_type.id}) }}">edit</a>

    {{ include('program_type/_delete_form.html.twig') }}
{% endblock %}
