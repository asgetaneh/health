<form method="post" action="{{ path('task_accomplishment_delete', {'id': task_accomplishment.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ task_accomplishment.id) }}">
    <button class="btn">Delete</button>
</form>
