{% extends 'base.html.twig' %}

{% block title %}InitiativeMeasurement{% endblock %}

{% block body %}
    <h1>InitiativeMeasurement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ initiative_measurement.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ initiative_measurement.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ initiative_measurement.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('initiative_measurement_index') }}">back to list</a>

    <a href="{{ path('initiative_measurement_edit', {'id': initiative_measurement.id}) }}">edit</a>

    {{ include('initiative_measurement/_delete_form.html.twig') }}
{% endblock %}
