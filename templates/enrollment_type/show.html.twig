{% extends 'base.html.twig' %}

{% block title %}EnrollmentType{% endblock %}

{% block body %}
    <h1>EnrollmentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enrollment_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ enrollment_type.name }}</td>
            </tr>
            <tr>
                <th>ShortName</th>
                <td>{{ enrollment_type.shortName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ enrollment_type.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('enrollment_type_index') }}">back to list</a>

    <a href="{{ path('enrollment_type_edit', {'id': enrollment_type.id}) }}">edit</a>

    {{ include('enrollment_type/_delete_form.html.twig') }}
{% endblock %}
