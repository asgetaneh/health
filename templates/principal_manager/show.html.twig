{% extends 'base.html.twig' %}

{% block title %}PrincipalManager{% endblock %}

{% block body %}
    <h1>PrincipalManager</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ principal_manager.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ principal_manager.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ principal_manager.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('principal_manager_index') }}">back to list</a>

    <a href="{{ path('principal_manager_edit', {'id': principal_manager.id}) }}">edit</a>

    {{ include('principal_manager/_delete_form.html.twig') }}
{% endblock %}
