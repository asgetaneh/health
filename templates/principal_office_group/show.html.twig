{% extends 'base.html.twig' %}

{% block title %}PrincipalOfficeGroup{% endblock %}

{% block body %}
    <h1>PrincipalOfficeGroup</h1>

    <table class="table">
        <tbody>
          
            <tr>
                <th>Name</th>
                <td>{{ principal_office_group.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ principal_office_group.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('principal_office_group_index') }}">back to list</a>

    <a href="{{ path('principal_office_group_edit', {'id': principal_office_group.id}) }}">edit</a>

    {{ include('principal_office_group/_delete_form.html.twig') }}
{% endblock %}
