<form method="post" action="{{ path('suitable_initiative_delete', {'id': suitable_initiative.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ suitable_initiative.id) }}">
    <button class="btn">Delete</button>
</form>
