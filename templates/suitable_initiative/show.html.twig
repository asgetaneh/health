{% extends 'base.html.twig' %}

{% block title %}SuitableInitiative{% endblock %}

{% block body %}
    <h1>SuitableInitiative</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ suitable_initiative.id }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ suitable_initiative.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('suitable_initiative_index') }}">back to list</a>

    <a href="{{ path('suitable_initiative_edit', {'id': suitable_initiative.id}) }}">edit</a>

    {{ include('suitable_initiative/_delete_form.html.twig') }}
{% endblock %}
